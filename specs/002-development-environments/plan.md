# Implementation plan

Own devenv files, container adapter/tests, portable runner mode and regression test, CI, ignores/archive exclusions, and contributor/planning documentation. Preserve QML runtime code, stock bar ownership, settings, release version, and full CI gates. Use existing portable model/contract tests on Linux and macOS; QML and live/stress tests stay with the full Linux/Omarchy workflow.

Validate native devenv, actual Podman, argument rejection, archive boundaries, and current-head Docker/macOS CI before protected delivery. No plugin release is needed for development tooling alone.
